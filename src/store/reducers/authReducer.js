const initState = {} //ovde pravimo pocetno stanje koje prosledjujemo state-u jer na pocetku nemamo podatke za state, ali mora da se prosledi makar i prazan objekat zbog f-je ispod

const authReducer = (state = initState, actions) => {
    return state
}

export default authReducer